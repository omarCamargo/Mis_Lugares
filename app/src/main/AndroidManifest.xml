<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omar.mislugares" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".AcercaDe"
            android:label="@string/header_about"
            android:theme="@style/Base.Theme.AppCompat.Dialog"/>
        <activity android:name=".Preferencias"
            android:label="@string/header_preferences"/>
        <activity android:name=".VistaLugar"
            android:label="@string/header_vistaLugar">
        </activity>
        <activity android:name=".EdicionLugar"
            android:label="@string/header_edicionLugar"/>




    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
</manifest>
